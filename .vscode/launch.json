{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Ansible Pod",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/ansible/playbook.yml"
        }
    ]
}